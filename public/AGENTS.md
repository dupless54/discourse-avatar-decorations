# Avatar decoration media

- `public/images` contains large binary assets: inspect only exact files relevant to the task.
- Do not bulk rewrite/recompress/rename assets without an explicit migration/update task.
- Preserve referenced filenames and compatibility with existing custom-field values.
- New assets must have clear provenance/licensing and reasonable web size/format; avoid embedding secrets or metadata not intended for publication.
